// npm - global command, comes with node
//npm --version check the version

//local dependency -- use it only in one particular project
// npm i <packagename>

//global dependency  - use it in any project
//npm install -g <packagename>

//npm i nodemon -D , --save-dev save the dependency //nodemon is a development dependency that automatically restarts your Node.js server whenever you change your code.
//--save-dev tells npm to install a package as a development dependency, not a production dependency.

//👉 In simple words:

//This package is only needed while developing the app, not when the app is running for users.

// uninstall : npm uninstall <packagename>